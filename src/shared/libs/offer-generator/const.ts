export enum Price {
  Min = 100,
  Max = 100000
}

export enum BedroomsCount {
  Min = 1,
  Max = 8
  }

export enum GuestsCount {
  Min = 1,
  Max = 10
}

export enum Rating {
  Min = 1,
  Max = 5
  }

export enum CommentsCount {
  Min = 0,
  Max = 100
}
